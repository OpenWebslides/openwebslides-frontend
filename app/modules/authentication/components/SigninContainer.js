// @flow

import * as React from 'react';
import { Container } from 'semantic-ui-react';

const SigninContainer = (): React.Node => {
  return (
    <Container>
      <p>Test</p>
    </Container>
  );
};

export default SigninContainer;
