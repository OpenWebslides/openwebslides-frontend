// @flow

import type { State } from 'types/state';
import type { Identifier } from 'types/model';


export const getTitleColor = (state: State, props: { id: Identifier }): string => {
  return '';
};

export const getBackgroundColor = (state: State, props: { id: Identifier }): string => {
  return '';
};
