// @flow

import { contentItemTypes } from 'modules/content-items';
import type { SlideStylingById } from './model';

export const dummySlideStylingById: SlideStylingById = {
  azd15dsqz1: {
    id: 'azd15dsqz1',
    userId: 'adkqmq5ds5',
    rules: {
      [contentItemTypes.PARAGRAPH]: {
        color: '#000000',
      },
      [contentItemTypes.HEADING]: {
        color: '#000000',
      },
    },
  },
};
